import SegmentationDashboard from "@/components/segmentation/SegmentationDashboard";

export const metadata = { title: "Segmentation — Moov Africa ML" };

export default function SegmentationPage() {
  return <SegmentationDashboard />;
}
