import FraudeDashboard from "@/components/fraude/FraudeDashboard";

export const metadata = { title: "Fraude — Moov Africa ML" };

export default function FraudePage() {
  return <FraudeDashboard />;
}
